# RustWatch web app dependencies
# Install with:  pip install -r requirements.txt

Flask>=3.0
PyMySQL>=1.1
ultralytics>=8.0        # YOLOv8 model (pulls in torch, numpy, etc.)
opencv-python>=4.8      # image handling (the "cv2" module)
numpy>=1.24
